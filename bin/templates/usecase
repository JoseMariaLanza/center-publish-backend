// Imports

export class {{ className }}StoreUseCase
{
    async store(req)
    {
        try {
            if (!{{ className }}Validation.validateStore(req)) {
                // return await ApiResponse.badRequest('HERE GOES YOUR CUSTOM ERROR!!.');
            }

            return {{ className }}Service.store(req);
        } catch (Exception ex) {
            return await ApiResponse.serverError(ex.message);
        }
    }
}
